# core

Querschnittscode fuer die ganze App.

Typische Inhalte:
- `config` fuer App-Konfiguration
- `constants` fuer zentrale Werte
- `errors` fuer Fehler- und Result-Modelle
- `routing` fuer Navigation
- `theme` fuer Designsystem und Styles
- `utils` fuer kleine, wiederverwendbare Helfer
